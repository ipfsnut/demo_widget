import React from 'react';

const HistoricalView = ({ dao }) => (
  <div className="historical-view">
    <h2>Governance History</h2>
    <p>Historical governance data for DAO {dao} will be shown here.</p>
  </div>
);

export default HistoricalView;