import React from 'react';

const VotingPowerCalculator = ({ dao }) => (
  <div className="voting-power-calculator">
    <h2>Your Voting Power</h2>
    <p>Voting power calculation for DAO {dao} will be displayed here.</p>
  </div>
);

export default VotingPowerCalculator;