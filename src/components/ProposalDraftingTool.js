import React from 'react';

const ProposalDraftingTool = ({ dao }) => (
  <div className="proposal-drafting-tool">
    <h2>Draft a Proposal</h2>
    <p>Tools for drafting proposals in DAO {dao} will be implemented here.</p>
  </div>
);

export default ProposalDraftingTool;