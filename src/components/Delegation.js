import React from 'react';

const Delegation = ({ dao }) => (
  <div className="delegation">
    <h2>Delegate Your Vote</h2>
    <p>Delegation options for DAO {dao} will be shown here.</p>
  </div>
);

export default Delegation;